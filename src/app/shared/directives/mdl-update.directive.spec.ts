import { MdlUpdateDirective } from './mdl-update.directive';

describe('MdlUpdateDirective', () => {
  it('should create an instance', () => {
    const directive = new MdlUpdateDirective();
    expect(directive).toBeTruthy();
  });
});
